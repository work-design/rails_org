<div class="ui top attached borderless menu">
  <div class="header item">Org Panel Job Titles</div>
  <div class="right menu">
    <div class="item">
      <%= link_to 'New Org Panel Job Title', new_panel_department_job_title_path(@department), class: 'ui teal button', remote: true %>
    </div>
  </div>
</div>

<%= render 'filter' %>

<table class="ui bottom attached table">
  <thead>
    <tr>
      <th><%= JobTitle.human_attribute_name(:id) %></th>
      <th><%= JobTitle.human_attribute_name(:department) %></th>
      <th><%= JobTitle.human_attribute_name(:name) %></th>
      <th><%= JobTitle.human_attribute_name(:grade) %></th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
  <% @job_titles.each do |job_title| %>
    <tr>
      <td><%= job_title.id %></td>
      <td><%= job_title.department.full_name %></td>
      <td><%= job_title.name %></td>
      <td><%= job_title.grade %></td>
      <td class="ui labels">
        <%= link_to edit_panel_department_job_title_path(@department, job_title), data: { tooltip: t('.edit') }, class: 'ui pink mini icon button', remote: true do %>
          <i class="pencil alternate icon"></i>
        <% end %>
        <%= link_to panel_department_job_title_path(@department, job_title), method: :delete, data: { tooltip: t('.destroy'), confirm: 'Are you sure?' }, class: 'ui red mini icon button' do %>
          <i class="times icon"></i>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<%= paginate @job_titles %>
