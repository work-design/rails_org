<%= render 'shared/error_messages', target: @department %>
<%= render partial: 'nodes/children_select', locals: { f: f, node: @roots } %>
<%= f.text_field :name, required: true %>
<%= f.number_field :needed_number %>
<%= f.file_field :logo %>
<%= f.collection_select :office_id, @offices, :id, :name, { include_blank: '无' } %>
<%= f.submit %>
