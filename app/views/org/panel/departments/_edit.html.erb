<div class="ui modal" id="modal">
  <i class="close icon"></i>
  <div class="ui header blue">
    Edit
  </div>

  <div class="content">
    <%= form_with model: @department, scope: :department, url: panel_department_path(@department) do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>
