<%=
  f.collection_select(
      :job_title_id,
      f.object.same_job_titles,
      :id,
      :name,
      { warp: { all: nil, select: 'select is-fullwidth' }, label: false },
      { data: { title: 'job_title_id', index: f.index } }
  )
%>
