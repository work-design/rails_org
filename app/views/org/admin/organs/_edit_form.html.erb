<%= form_with model: [:admin, @organ] do |f| %>
  <%= render 'shared/error_messages', target: @organ %>
  <%= render partial: 'taxon/nodes/children_select', locals: { f: f, node: current_organ } %>
  <%= render partial: 'form', locals: { f: f } %>
<% end %>
