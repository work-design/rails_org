<td>
  <div class="is-several pl-6">
    <% if !model.leaf? %>
      <i class="fa-solid fa-caret-down fa-fw" data-action="click->tree#collapse" style="cursor: pointer"></i>
    <% else %>
      <i class="fa-solid fa-minus fa-fw"></i>
    <% end %>
    <%= image_tag model.logo.variant(resize_to_limit: [30, 30]), class: 'image is-24x24' if model.logo.attached? %>
    <span><%= model.name %></span>
    <% if model.name_short.present? %>
      <span>（<%= model.name_short %>）</span>
    <% end %>
  </div>
</td>
<td><%= model.code %></td>
<td>
  <p><%= model.area&.full_name if defined?(RailsShip) %></p>
  <p><%= model.address %></p>
</td>
<td>
  <div class="tags mb-0">
    <% model.roles.pluck(:name).each do |role| %>
      <span class="tag"><%= role %></span>
    <% end %>
  </div>
  <div class="mt-0">
    <%= button_to({ action: 'edit_roles', id: model.id }) do %>
      <i class="fa-solid fa-user-shield text-link"></i>
    <% end %>
  </div>
</td>
