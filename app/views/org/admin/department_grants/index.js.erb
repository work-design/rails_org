$('#modal').replaceWith('<%= j(render 'index') %>');
$('#modal').modal({
  onHide: function(element) {
    remote_js_load('<%= params[:return_to] %>');
  }
}).modal('show');
remote_js_load('<%= raw remote_js_load %>')
