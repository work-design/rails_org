<%= form_with theme: 'search', model: Org::Support.new do |f| %>
  <div class="fields">
    <%= f.collection_select :organ_id, Org::Organ.all, :id, :name, { selected: params[:office_id] } %>
    <%= f.submit %>
  </div>
<% end %>


