<div class="cards mx-3">
  <div id="new_card">
  <% @roles.each do |role| %>
    <%= button_to({ controller: 'organs', action: 'new', role_id: role.id }, class: 'button is-fullwidth is-primary mt-3') do %>
      <i class="fa-solid fa-plus"></i>
      <span class="ml-1"><%= role.tip %></span>
    <% end %>
  <% end %>
  </div>
</div>