<%= content_for :breadcrumb do %>
<div class="breadcrumb has-succeeds-separator">
  <ul>
    <li class="is-active"><%= link_to t('org.member.departments.index.title'), me_departments_path %></li>
  </ul>
</div>
<% end %>

<%= render 'filter_form' %>

<div class="columns">
  <div class="column">
    <table class="table is-hoverable is-fullwidth">
      <thead>
        <tr>
          <th><%= Member.human_attribute_name(:name) %></th>
          <th><%= Member.human_attribute_name(:job_titles) %></th>
          <th><%= Member.human_attribute_name(:organ) %></th>
          <th><%= Member.human_attribute_name(:join_on) %></th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <% @members.each do |member| %>
        <%= render partial: 'member', locals: { member: member } %>
      <% end %>
      </tbody>
    </table>
  
    <%= paginate @members %>
  </div>
  <div class="column is-narrow" style="width: 260px;">
    <%= render 'nav_show' %>
  </div>
</div>



