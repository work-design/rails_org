<%= form_with theme: 'search', model: Org::Department.new do |f| %>
  <div class="field-body">
    <%= f.text_field 'name-like' %>
    <div class="field is-narrow">
      <%= f.submit %>
      <%= link_to t('.clear'), filter_params(except: [:'name-like', :identity]), class: 'button is-light' %>
    </div>
  </div>
<% end %>
