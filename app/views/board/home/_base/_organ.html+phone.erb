<%= link_to({ controller: organ.redirect_controller, action: organ.redirect_action, auth_token: current_authorized_token&.id, host: organ.host }, class: 'media') do %>
  <div class="media-content">
    <span><%= organ.name %></span>
  </div>
  <div class="media-right">
    <i class="fa-solid fa-chevron-right"></i>
  </div>
<% end %>
